interface:
  display_name: "Corply"
  short_description: "Incorporate and run your company with Corply."
  brand_color: "#10A37F"
  default_prompt: "Use $corply to work my company's highest-priority next action."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "corply"
      description: "Corply company formation and operations MCP server"
      transport: "streamable_http"
      url: "https://corply.dev/mcp"
